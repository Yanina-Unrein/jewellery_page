---
const { icon, alt, content } = Astro.props;
---
<div class="contact-info">
  <img src={icon} alt={alt} class="contact-icon" />
  <p>{content}</p>
</div>

<style>
  .contact-info {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: 10px;
  }

  p{
    font-size: clamp(1rem, 0.9825rem + 0.0877vw, 1.125rem);
  }

  .contact-icon {
    width: 40px;
    height: 40px;
  }
</style>