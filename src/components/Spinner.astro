---
const { size = "100px" } = Astro.props;
---

<div class="spinner-overlay">
  <div class="spinner-container">
    <img 
      src="/iconLogo/logo_gold_completo.svg" 
      alt="Spinner Logo Melo" 
      class="logo-spinner"
      style={`width: ${size}; height: ${size};`}
    />
  </div>
</div>

<style>
  .spinner-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: rgba(255, 255, 255, 0.8);
    z-index: 9999;
    pointer-events: none; /* Permite interactuar con elementos debajo */
  }
</style>